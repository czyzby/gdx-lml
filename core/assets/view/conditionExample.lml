<table>
<@if 11 % 5 = 1>
    <@if aaab regex a+[bc]>
        <@test ${checkedArgument} <= 5.7>
            Condition macros might have problems with nested conditions ("else" blocks in particular), so you can use aliases.
        <@test:else/>
            This should be printed.
        </@test>
    </@if>
</@if>
</table>