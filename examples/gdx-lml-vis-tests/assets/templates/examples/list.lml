<!-- Lists convert plain text, labels and
    text buttons into their lines. -->

<list selected=2>
    Some line.
    <label text="Also valid." />
    2 - should be selected.
    <label color=vis-red>Attributes are ignored.</label>
    <textButton>This is because list</textButton>
    extracts text from children
    and converts it to its lines.
</list>